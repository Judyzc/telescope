{
  "name": "telescope-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev dist --r2=UNZIP_BUCKET --compatibility-date=2024-01-01",
    "build": "node build.js",
    "deploy": "npm run build && wrangler pages deploy dist",
    "preview": "npm run build && npm run dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.59.2"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "hono": "^4.7.0"
  }
}
